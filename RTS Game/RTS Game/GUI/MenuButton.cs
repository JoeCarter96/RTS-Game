using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

/*
namespace RTS_Game.GUI
{
    class MenuButton : Component
    {
        private MainMenuState context;
        private String state;

        public MenuButton(Texture2D texture, Vector2 position, Rectangle size, MainMenuState context, String state)
            : base(texture, position, new Size(size))
        {
            this.context = context;
            this.state = state;
        }

        public override void Draw(SpriteBatch spriteBatch)
        {
            base.Draw(spriteBatch);
        }

        public override void MouseClicked(int x, int y, MouseButton button)
        {
            if (button == MouseButton.Left)
            {
            }
        }
    } 
}
 * */
